# Legacy Phase 1

This directory preserves the exploratory phase-1 prompting work:

- ad hoc extraction and conversion scripts
- early synthetic corpora and tagged outputs
- intermediate JSONL and text artifacts
- archive material used during pilot experiments

Nothing here should be treated as the foundation for the next pipeline without explicit migration. New work should target `src/icl_pilot/`, `configs/`, and `data/`.
